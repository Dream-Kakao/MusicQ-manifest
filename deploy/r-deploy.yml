apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe
  template:
    metadata:
      labels:
        app: fe
    spec:
      containers:
      - name: test
        image: leehb97/testraction:b6223f8c65866738e68433fd5e68dca323e92dce
        
---

apiVersion: v1
kind: Service
metadata:
  name: fe-svc
spec:
  selector:
    app: fe
  ports:
  - name: http
    port: 3050
    protocol: TCP
    targetPort: 80
  type: LoadBalancer
